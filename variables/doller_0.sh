#!/bin/bash

echo "$ 0 is used to referance the script itself"
echo "this is used to print the name of the script running like that"
echo "if needed we can delete the script or self distruct after execution completed"

echo " "

echo "file name: $0"

# rm -f $0 if needed to delete
